#include <stdio.h>
#include <conio.h>
void main()
{
    int number;
    printf("enter the number");
    scanf("%d",&number);
    if(n%==2)
    printf("even");
    else
    printf("odd");

    return 0;
}
